package com.revising;

public class Main {

    public static void main(String[] args) {
	// declare and initialize variables
        // squareMeters
        // hasTV
        // hasCouch
        // windows

        float sqaureMeters;
        boolean hasTV = true;
        boolean hasCouch = false;
        int windows;

        sqaureMeters = 56.3f;

        windows = 8;

        System.out.println("My house has " + (sqaureMeters) + " squaremeters" + ", " + "it is " + hasTV + " that it has TV and " + hasCouch + " that it has couch. " + "Number of windows is " + windows + ".");

    }
}
